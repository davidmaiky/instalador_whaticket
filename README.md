## 💽 Instalação

<p>Copie e cole no Terminal da sua VPS:</p>

```
sudo apt install -y git && git clone https://github.com/davidmaiky/instalador_whaticket.git instalador && sudo chmod -R 777 instalador && cd instalador && sudo ./install_primaria
```
